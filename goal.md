**Findings**

1. **High: Business plan budget limits are internally inconsistent.** `Business` allows 500 budget items in [convex/entitlements.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/entitlements.ts:53), but setup rejects more than 200 before entitlement checks in [convex/setup.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/setup.ts:50). This makes the paid plan less capable than documented in [docs/saas-standardization.md](/home/terasumi/Documents/source_code/Web/li-xi/docs/saas-standardization.md:92).

2. **High: “Budget per campaign” is not explicit enough.** The product flow says campaign setup precedes budget/session creation in [README.md](/home/terasumi/Documents/source_code/Web/li-xi/README.md:10), but setup resolves budget scope implicitly from the active/default campaign and exposes no campaign selector or `campaignId` arg in [convex/setup.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/setup.ts:328) and [convex/setup.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/setup.ts:369). For a SaaS prize platform, make campaign-scoped budget setup a first-class workflow.

3. **High: The “brand” campaign theme is mostly a label today.** Campaign Studio lets users choose `brand` in [app/campaigns.tsx](/home/terasumi/Documents/source_code/Web/li-xi/app/campaigns.tsx:1848), but the draw template registry only supports `"li-xi"` in [app/draw/templates/types.ts](/home/terasumi/Documents/source_code/Web/li-xi/app/draw/templates/types.ts:5), and routes load `getDrawTemplate()` without using campaign theme in [app/draw.tsx](/home/terasumi/Documents/source_code/Web/li-xi/app/draw.tsx:60). Either make brand theming real or hide the choice until it is supported.

4. **Medium: Draw/session policy logic is duplicated across backend modules.** Pending-session delivery buckets and lookup helpers repeat in [convex/draw.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/draw.ts:43), [convex/setup.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/setup.ts:48), and [convex/campaigns.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/campaigns.ts:43). Extract a shared `drawSessionPolicy`/`pendingSessions` helper to reduce divergence risk.

5. **Medium: `app/campaigns.tsx` is doing too much.** It is 2,259 lines and owns form state, upload transport, billing, readiness, analytics, preview, list, editor tabs, and action bar. Split into route container plus `CampaignEditor`, `CampaignAssetsPanel`, `BillingPanel`, `ReadinessPanel`, and focused hooks.

6. **Medium: legacy optional campaign scope remains throughout the data model.** `campaignId` is still optional on budgets, assets, sessions, redemptions, and analytics in [convex/schema.ts](/home/terasumi/Documents/source_code/Web/li-xi/convex/schema.ts:49). That is valid for migration, but the plan already calls out backfill/removal work in [docs/saas-standardization.md](/home/terasumi/Documents/source_code/Web/li-xi/docs/saas-standardization.md:81). After migration, tighten invariants so new SaaS records are campaign-scoped by default.

7. **Medium: tests are strong on contracts, weak on real workflows.** The repo has many policy/contract scripts, but the smoke test only checks rendered route text in [scripts/smoke-routes.mjs](/home/terasumi/Documents/source_code/Web/li-xi/scripts/smoke-routes.mjs:18). This matches the remaining-work item for full browser/database E2E in [docs/saas-standardization.md](/home/terasumi/Documents/source_code/Web/li-xi/docs/saas-standardization.md:86). Add Playwright or Convex-backed E2E for OAuth completion, campaign creation, budget setup, station draw, public claim redeem, and entitlement failures.

8. **Low: `draw.css` still carries generic app/admin semantic tokens.** The draw design system defines a focused red/gold skin in [design_system.md](/home/terasumi/Documents/source_code/Web/li-xi/design_system.md:21), but [app/styles/draw.css](/home/terasumi/Documents/source_code/Web/li-xi/app/styles/draw.css:3) still defines broad `--background`, `--card`, `--sidebar`, chart, and dark-mode tokens. Remove unused semantic/admin tokens from draw CSS to keep the two style domains cleaner.

9. **Low: `update_ui_plan.md` is stale.** It says `@heroui/react` and `@heroui/styles` are missing in [update_ui_plan.md](/home/terasumi/Documents/source_code/Web/li-xi/update_ui_plan.md:13), but they exist in [package.json](/home/terasumi/Documents/source_code/Web/li-xi/package.json:27). Either archive it as historical context or update it into a current migration checklist.

10. **Low: upload UI copy underreports allowed formats.** Policy allows JPG, PNG, WebP, GIF, and AVIF in [lib/assetPolicy.ts](/home/terasumi/Documents/source_code/Web/li-xi/lib/assetPolicy.ts:4), while the DropZone says only JPG, PNG, or WebP in [app/campaigns.tsx](/home/terasumi/Documents/source_code/Web/li-xi/app/campaigns.tsx:2050).

No files were changed. Verification passed: `npm run typecheck`, `npm run test:contracts`, `npm run lint`, `npm run test:smoke`, and `npm audit --omit=dev`.

Remaining risk: I reviewed current code and static/runtime smoke behavior, but did not exercise a live Convex deployment, Google OAuth, R2 upload, or Polar checkout. Goal marked complete; tool usage was 235,288 tokens over about 5m 7s.
